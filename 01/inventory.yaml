---

webservers:
  hosts:
    webserver01:
      ansible_host: x.x.x.x
  vars:
    ansible_connection: ssh
    ansible_user: ubuntu
    ansible_port: PORT
    ansible_ssh_private_key_file: ~/.ssh/KEY

